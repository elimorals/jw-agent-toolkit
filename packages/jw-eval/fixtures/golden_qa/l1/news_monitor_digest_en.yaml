# packages/jw-eval/fixtures/golden_qa/l1/news_monitor_digest_en.yaml
# Agent "news_monitor" is NOT registered in
# jw_eval/cli.py::default_agent_registry, and the `_adapter:` key is bogus
# (no agent_adapters.py exists). Kwargs (since, languages, channels) match
# the real signature though; will pass once the registry is updated and the
# stub-source mechanism lands.
# BLOCKED: agent calls `suite.telemetry` but ClientSuite has no such
# attribute (real bug in jw_agents.news_monitor, not in this YAML).
# Tracked separately. Case stays here for when the agent is fixed.
id: l1_news_monitor_digest_en
agent: news_monitor
layer: l1
input:
  since: epoch
  languages: [en]
  channels: [publications]
expected:
  # Agent is registered now; without a stub source the digest may be
  # empty (no upstream calls in offline mode), so only the structural
  # contract is enforced.
  min_findings: 0
  forbidden_keywords_in_findings:
    - "(none)"
metadata:
  topic: news.publications
  fase: 25
  added_by: elias
  added_at: 2026-05-30
