=== .github/workflows/ci.yml ===
name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - run: uv python install 3.13
      - run: uv sync
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest -v

=== .gitignore ===
.venv/
__pycache__/
*.pyc
*.pyo
dist/
build/
*.egg-info/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.tox/

=== Makefile ===
.PHONY: install test lint format ci

install:
	uv sync

test:
	uv run pytest -v

lint:
	uv run ruff check .

format:
	uv run ruff format .

ci: lint test

=== README.md ===
# my-vlm

An agent plugin for [`jw-agent-toolkit`](https://github.com/eliascipre/jw-agent-toolkit) (Fase 41 plugin SDK).

## Install (editable)

```bash
uv sync
uv run pytest
```

## Verify discovery

```bash
uv pip install -e .
uv run jw plugins list
uv run jw plugins verify my_vlm
```

## Implement your logic

Edit `src/my_vlm/agent.py`. The stub returns a placeholder finding;
replace with real calls to `jw-core` / `jw-rag` / `jw-agents`.

See [cookbook recipes](https://github.com/eliascipre/jw-agent-toolkit/tree/main/docs/cookbook)
for copy-pasteable examples.

## Scope reminder

`jw-agent-toolkit` is 100% for Jehovah's Witnesses publications. Build plugins
that serve the JW community: meeting prep, apologetics, ministry tools,
specialized parsers for JW formats, etc.

## License

GPL-3.0

=== pyproject.toml ===
[project]
name = "my-vlm"
version = "0.1.0"
description = "A VLM (vision+language) provider plugin for jw-agent-toolkit."
readme = "README.md"
requires-python = ">=3.13"
license = "GPL-3.0"
dependencies = [
    "jw-core>=2.3,<3.0",
]

[project.entry-points."jw_agent_toolkit.vlm_providers"]
my_vlm = "my_vlm.vlm:My_vlmVLM"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.7",
]

[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/my_vlm"]

=== src/my_vlm/__init__.py ===
"""my-vlm — VLM provider plugin for jw-agent-toolkit."""

from __future__ import annotations

from my_vlm.vlm import My_vlmVLM

__all__ = ["My_vlmVLM"]
__version__ = "0.1.0"

=== src/my_vlm/vlm.py ===
"""my-vlm — stub VLM provider. Replace with real vision model integration.

Required Protocol shape:
  - name: str
  - is_available() -> bool
  - describe(image_bytes: bytes, *, language: str = "en") -> str
"""

from __future__ import annotations


class My_vlmVLM:
    name = "my_vlm"

    def is_available(self) -> bool:
        return True

    def describe(self, image_bytes: bytes, *, language: str = "en") -> str:
        # Stub: returns deterministic placeholder. Replace with model call.
        return f"my_vlm[{language}] len={len(image_bytes)}"

=== tests/__init__.py ===
"""Tests for my-vlm."""

=== tests/conftest.py ===
"""Shared fixtures for my-vlm tests.

By design these are deterministic and do NOT touch the network.
Add your fakes here (FakeWOLClient, FakeEmbedder, etc.) as you grow.
"""

from __future__ import annotations

=== tests/test_my_vlm.py ===
"""Tests for my-vlm VLM provider. Deterministic, no network, no model."""

from __future__ import annotations

from my_vlm.vlm import My_vlmVLM


def test_attributes_present() -> None:
    vlm = My_vlmVLM()
    assert vlm.name == "my_vlm"


def test_is_available() -> None:
    assert My_vlmVLM().is_available()


def test_describe_returns_string() -> None:
    out = My_vlmVLM().describe(b"x" * 100, language="es")
    assert isinstance(out, str)
    assert "100" in out
    assert "es" in out
