"""{{ name }} — stub generation provider. Replace with real LLM integration.

Required Protocol shape:
  - name: str
  - is_available() -> bool
  - generate(prompt: str, *, max_tokens: int = 128) -> str
"""

from __future__ import annotations


class {{ module|capitalize }}Gen:
    name = "{{ module }}"

    def is_available(self) -> bool:
        return True

    def generate(self, prompt: str, *, max_tokens: int = 128) -> str:
        # Stub: returns deterministic placeholder. Replace with LLM call.
        return f"{{ module }}[max_tokens={max_tokens}]: {prompt[:max_tokens]}"
